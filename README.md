# Messing around with React SPA

## useParams and Passing as Query param to API

## ReactStateFromURLQueryParams
A way to get the state from the url query params - useful for filtering from a database
